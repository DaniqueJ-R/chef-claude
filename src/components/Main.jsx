export default function Header() {
    return(
        <main>
            <section className="add-ingredient-form">
                <input type="" placeholder="e.g. salmon" />
                <button type="submit"> Add ingredient</button>
            </section>
            
        </main>
    )
}